import React from 'react';
import Icons from 'react-native-vector-icons/EvilIcons';
import {IconProps} from 'react-native-vector-icons/Icon';
const EvilIcon = (props: IconProps) => <Icons {...props} />;

export default EvilIcon;
